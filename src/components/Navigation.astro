---
// Navigation component for Sea OKI Collection
// Implements unified font hierarchy and high-visibility Book Now CTA
---

<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm sticky-top">
    <div class="container">
        <a class="navbar-brand" href="/"> Sea OKI Collection </a>

        <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarNav"
            aria-controls="navbarNav"
            aria-expanded="false"
            aria-label="Toggle navigation"
        >
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto align-items-center">
                <li class="nav-item">
                    <a class="nav-link" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/properties">Properties</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/dining">Dining</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/blog">Blog</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/about">About</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/contact">Contact</a>
                </li>
                <li class="nav-item ms-lg-3">
                    <a class="btn btn-book-now" href="https://www.houfy.com/lodging/steps-to-the-sea-walk-to-the-beach-oak-island/79833?guests=1&adults=1&children=0" target="_blank" rel="noopener noreferrer">Book Now</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<style>
    /* 
     * Navigation specific styles with unified font hierarchy
     * CSS custom properties are defined in src/styles/theme.css:
     * --font-header, --font-subheader, --font-body
     * --color-primary, --color-secondary, --color-accent, --color-warm
     */
    .navbar-brand {
        font-family: var(--font-header);
        font-weight: 700;
        font-size: 1.75rem;
        color: var(--color-primary) !important;
        letter-spacing: 0.02em;
    }

    .nav-link {
        font-family: var(--font-subheader);
        font-size: 1rem;
        font-weight: 400;
        color: var(--color-primary) !important;
        padding: 0.5rem 1rem !important;
        transition: color 0.3s ease;
    }

    .nav-link:hover {
        color: var(--color-secondary) !important;
    }

    /* High-visibility Book Now CTA */
    .btn-book-now {
        font-family: var(--font-subheader);
        font-size: 1.1rem;
        font-weight: 500;
        background-color: var(--color-accent);
        border: 2px solid var(--color-accent);
        color: white;
        padding: 0.75rem 2rem;
        border-radius: 50px;
        transition: all 0.3s ease;
        /* Shadow color matches accent color (#F09979) with 30% opacity */
        box-shadow: 0 4px 12px color-mix(in srgb, var(--color-accent) 30%, transparent);
        text-transform: uppercase;
        letter-spacing: 0.05em;
    }

    .btn-book-now:hover {
        background-color: var(--color-primary);
        border-color: var(--color-primary);
        color: white;
        /* Shadow color matches primary color (#172338) with 40% opacity */
        box-shadow: 0 6px 16px color-mix(in srgb, var(--color-primary) 40%, transparent);
        transform: translateY(-2px);
    }

    /* Responsive adjustments */
    @media (max-width: 991.98px) {
        .btn-book-now {
            display: block;
            width: 100%;
            margin-top: 1rem;
        }

        .nav-item.ms-lg-3 {
            margin-left: 0 !important;
        }
    }
</style>
