------

import { type CollectionEntry, getCollection } from 'astro:content';import { type CollectionEntry, getCollection, render } from "astro:content";

import Layout from '../../layouts/Layout.astro';import Layout from "../../layouts/Layout.astro";

import Navigation from '../../components/Navigation.astro';import Navigation from "../../components/Navigation.astro";

import Footer from '../../components/Footer.astro';import Footer from "../../components/Footer.astro";



export async function getStaticPaths() {export async function getStaticPaths() {

	const posts = await getCollection('blog');	const posts = await getCollection("blog");

	return posts.map((post) => ({	return posts.map((post) => ({

		params: { slug: post.id },		params: { slug: post.id },

		props: post,		props: post,

	}));	}));

}}

type Props = CollectionEntry<'blog'>;type Props = CollectionEntry<"blog">;



const post = Astro.props;const post = Astro.props;

const { Content } = await post.render();const { Content } = await render(post);



function formatDate(date: Date) {function formatDate(date: Date) {

	return date.toLocaleDateString('en-US', {	return date.toLocaleDateString("en-US", {

		year: 'numeric',		year: "numeric",

		month: 'long',		month: "long",

		day: 'numeric'		day: "numeric",

	});	});

}}

------



<Layout title={post.data.title} description={post.data.description}><Layout

	<Navigation />	title={`${post.data.title} | Sea OKI Collection Blog`}

		description={post.data.description}

	<article class="container py-5">>

		<!-- Hero Image -->	<Navigation />

		{post.data.heroImage && (

			<div class="row mb-5">	<article class="container py-5">

				<div class="col-12">		<!-- Breadcrumb -->

					<img 		<nav aria-label="breadcrumb" class="mb-4">

						src={post.data.heroImage.src} 			<ol class="breadcrumb">

						alt={post.data.title}				<li class="breadcrumb-item">

						class="img-fluid rounded w-100"					<a href="/" class="text-decoration-none">Home</a>

						style="height: 400px; object-fit: cover;"				</li>

					/>				<li class="breadcrumb-item">

				</div>					<a href="/blog" class="text-decoration-none">Blog</a>

			</div>				</li>

		)}				<li class="breadcrumb-item active" aria-current="page">

					{post.data.title}

		<!-- Article Header -->				</li>

		<div class="row justify-content-center">			</ol>

			<div class="col-lg-8">		</nav>

				<header class="text-center mb-5">

					<h1 class="display-4 fw-bold brand-text mb-3">{post.data.title}</h1>		<!-- Article Header -->

					<div class="text-muted mb-3">		<header class="text-center mb-5">

						<time datetime={post.data.pubDate.toISOString()}>			<h1 class="display-4 fw-bold mb-3">{post.data.title}</h1>

							{formatDate(post.data.pubDate)}			<p class="lead text-muted mb-4">{post.data.description}</p>

						</time>			<div class="mb-4">

						{post.data.updatedDate && (				<small class="text-muted"

							<span> • Updated: {formatDate(post.data.updatedDate)}</span>					>Published on {formatDate(post.data.pubDate)}</small

						)}				>

					</div>			</div>

					<p class="lead">{post.data.description}</p>			{

				</header>				post.data.heroImage && (

					<img

				<!-- Article Content -->						src={post.data.heroImage}

				<div class="prose prose-lg mx-auto">						alt={post.data.title}

					<Content />						class="img-fluid rounded shadow-lg mb-5"

				</div>						style="max-height: 400px; width: 100%; object-fit: cover;"

					/>

				<!-- Back to Blog -->				)

				<div class="text-center mt-5 pt-5">			}

					<a href="/blog" class="btn btn-outline-brand">		</header>

						← Back to Blog

					</a>		<!-- Article Content -->

				</div>		<div class="row justify-content-center">

			</div>			<div class="col-lg-8">

		</div>				<div class="prose prose-lg mx-auto">

	</article>					<Content />

				</div>

	<Footer />			</div>

</Layout>		</div>



<style>		<!-- Article Footer -->

	.prose {		<footer class="mt-5 pt-5 border-top">

		max-width: none;			<div class="row">

	}				<div class="col-lg-8 mx-auto">

						<div class="text-center">

	.prose h2 {						<h4 class="fw-bold mb-3">

		color: var(--brand-color);							Ready to Plan Your Oak Island Getaway?

		margin-top: 2rem;						</h4>

		margin-bottom: 1rem;						<p class="mb-4">

	}							Let us help you create the perfect vacation

								experience at one of our beautiful properties.

	.prose h3 {						</p>

		margin-top: 1.5rem;						<div

		margin-bottom: 0.75rem;							class="d-flex justify-content-center gap-3 flex-wrap"

	}						>

								<a href="/properties" class="btn btn-brand"

	.prose p {								>View Our Properties</a

		margin-bottom: 1.25rem;							>

		line-height: 1.7;							<a href="/contact" class="btn btn-outline-primary"

	}								>Contact Us</a

								>

	.prose ul, .prose ol {							<a href="/blog" class="btn btn-outline-secondary"

		margin-bottom: 1.25rem;								>More Articles</a

	}							>

							</div>

	.prose li {					</div>

		margin-bottom: 0.5rem;				</div>

	}			</div>

			</footer>

	.prose blockquote {	</article>

		border-left: 4px solid var(--brand-color);

		padding-left: 1rem;	<Footer />

		margin: 1.5rem 0;</Layout>

		font-style: italic;

		background-color: #f8f9fa;<style>

		padding: 1rem;	.prose {

		border-radius: 0.5rem;		max-width: none;

	}	}

</style>
	.prose h2 {
		color: var(--brand-color);
		font-weight: bold;
		margin-top: 2rem;
		margin-bottom: 1rem;
	}

	.prose h3 {
		font-weight: 600;
		margin-top: 1.5rem;
		margin-bottom: 0.75rem;
	}

	.prose p {
		margin-bottom: 1rem;
		line-height: 1.6;
	}

	.prose ul,
	.prose ol {
		margin-bottom: 1rem;
		padding-left: 1.5rem;
	}

	.prose li {
		margin-bottom: 0.5rem;
	}

	.prose blockquote {
		border-left: 4px solid var(--brand-color);
		padding-left: 1rem;
		margin: 1.5rem 0;
		font-style: italic;
		background-color: #f8f9fa;
		padding: 1rem;
		border-radius: 0.375rem;
	}

	.prose strong {
		font-weight: 600;
		color: var(--brand-color);
	}
</style>
